{
  "name": "html-custom-elements",
  "version": "1.0.0",
  "description": "Creating of custom HTML elements",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files '**/*.css, **/*.html' --directory --port 8888"
  },
  "author": "Josef Lexa",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.24.6"
  }
}
