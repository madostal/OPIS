{
  "name": "aria-testing",
  "version": "1.0.0",
  "description": "testing of the aria standards",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files '**/*.css, **/*.html' --directory --port 7777"
  },
  "author": "Josef Lexa",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.24.6",
    "focus-visible": "^4.1.5",
    "wicg-inert": "^2.0.0"
  }
}
